---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: eventresponses.security.ugallu.io
spec:
  group: security.ugallu.io
  names:
    kind: EventResponse
    listKind: EventResponseList
    plural: eventresponses
    shortNames:
    - er
    singular: eventresponse
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.action.type
      name: Action
      type: string
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .status.outcome.type
      name: Outcome
      type: string
    - jsonPath: .spec.responder.name
      name: Responder
      type: string
    - jsonPath: .status.attempts
      name: Attempts
      type: integer
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          EventResponse records a single atomic action taken by a responder.
          Cluster-scoped (D5). Listing per parent uses label
          `ugallu.io/security-event-uid=<uid>`.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: |-
              EventResponseSpec describes a single atomic action taken by a responder
              in reaction to a SecurityEvent. Spec is immutable post-creation
              (enforced by admission policy, design 15).
            properties:
              action:
                description: Action is the typed action with parameters.
                properties:
                  parameters:
                    additionalProperties:
                      type: string
                    type: object
                  targetRef:
                    description: ObjectReference contains enough information to let
                      you inspect or modify the referred object.
                    properties:
                      apiVersion:
                        description: API version of the referent.
                        type: string
                      fieldPath:
                        description: |-
                          If referring to a piece of an object instead of an entire object, this string
                          should contain a valid JSON/Go field access statement, such as desiredState.manifest.containers[2].
                          For example, if the object reference is to a container within a pod, this would take on a value like:
                          "spec.containers{name}" (where "name" refers to the name of the container that triggered
                          the event) or if no container name is specified "spec.containers[2]" (container with
                          index 2 in this pod). This syntax is chosen only to have some well-defined way of
                          referencing a part of an object.
                        type: string
                      kind:
                        description: |-
                          Kind of the referent.
                          More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
                        type: string
                      name:
                        description: |-
                          Name of the referent.
                          More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                        type: string
                      namespace:
                        description: |-
                          Namespace of the referent.
                          More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/namespaces/
                        type: string
                      resourceVersion:
                        description: |-
                          Specific resourceVersion to which this reference is made, if any.
                          More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#concurrency-control-and-consistency
                        type: string
                      uid:
                        description: |-
                          UID of the referent.
                          More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#uids
                        type: string
                    type: object
                    x-kubernetes-map-type: atomic
                  type:
                    description: |-
                      ActionType is the kind of action an EventResponse takes (design 04 Q4).
                      Adding values requires a CRD bump and an admission policy update.
                    enum:
                    - PodFreeze
                    - PodUnfreeze
                    - NetworkDeny
                    - FilesystemSnapshot
                    - MemorySnapshot
                    - EvidenceUpload
                    - GitOpsChange
                    - RBACRevoke
                    - NodeIsolate
                    - ImageQuarantine
                    - WebhookDisable
                    - TokenRevoke
                    - NotificationSend
                    type: string
                required:
                - type
                type: object
              parentResponseRef:
                description: |-
                  ParentResponseRef chains responses for multi-step workflows
                  (e.g., forensics IncidentResponse expands into multiple EventResponses).
                properties:
                  apiVersion:
                    description: API version of the referent.
                    type: string
                  fieldPath:
                    description: |-
                      If referring to a piece of an object instead of an entire object, this string
                      should contain a valid JSON/Go field access statement, such as desiredState.manifest.containers[2].
                      For example, if the object reference is to a container within a pod, this would take on a value like:
                      "spec.containers{name}" (where "name" refers to the name of the container that triggered
                      the event) or if no container name is specified "spec.containers[2]" (container with
                      index 2 in this pod). This syntax is chosen only to have some well-defined way of
                      referencing a part of an object.
                    type: string
                  kind:
                    description: |-
                      Kind of the referent.
                      More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
                    type: string
                  name:
                    description: |-
                      Name of the referent.
                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                    type: string
                  namespace:
                    description: |-
                      Namespace of the referent.
                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/namespaces/
                    type: string
                  resourceVersion:
                    description: |-
                      Specific resourceVersion to which this reference is made, if any.
                      More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#concurrency-control-and-consistency
                    type: string
                  uid:
                    description: |-
                      UID of the referent.
                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#uids
                    type: string
                type: object
                x-kubernetes-map-type: atomic
              responder:
                description: Responder identifies the controller emitting this response.
                properties:
                  instance:
                    type: string
                  kind:
                    type: string
                  name:
                    type: string
                  version:
                    type: string
                required:
                - kind
                - name
                type: object
              retryPolicy:
                description: RetryPolicy controls retry on transient errors.
                properties:
                  backoffStrategy:
                    default: Exponential
                    description: BackoffStrategy is the retry backoff strategy.
                    enum:
                    - Exponential
                    - Fixed
                    - None
                    type: string
                  initialDelay:
                    description: InitialDelay (default 10s).
                    type: string
                  maxAttempts:
                    default: 3
                    type: integer
                  maxDelay:
                    description: MaxDelay (default 5m).
                    type: string
                type: object
              securityEventRef:
                description: SecurityEventRef is the parent SecurityEvent this response
                  acts on.
                properties:
                  name:
                    type: string
                  uid:
                    description: |-
                      UID is a type that holds unique ID values, including UUIDs.  Because we
                      don't ONLY use UUIDs, this is an alias to string.  Being a type captures
                      intent and helps make sure that UIDs and names do not get conflated.
                    type: string
                required:
                - name
                type: object
            required:
            - action
            - responder
            - securityEventRef
            type: object
          status:
            description: EventResponseStatus is the mutable lifecycle of the response.
            properties:
              attempts:
                type: integer
              cancelReason:
                type: string
              completedAt:
                format: date-time
                type: string
              conditions:
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              error:
                description: ResponseError records a typed failure with retry classification.
                properties:
                  detail:
                    type: string
                  reason:
                    type: string
                  type:
                    description: ErrorCategory classifies an EventResponse error for
                      retry decisioning (design 04 Q11).
                    enum:
                    - Transient
                    - Permanent
                    - Conflicting
                    - Unauthorized
                    type: string
                required:
                - type
                type: object
              evidence:
                items:
                  description: EvidenceRef points at an immutable blob stored in WORM
                    (or external for URLs).
                  properties:
                    mediaType:
                      type: string
                    sha256:
                      type: string
                    size:
                      format: int64
                      type: integer
                    url:
                      type: string
                  required:
                  - mediaType
                  - url
                  type: object
                type: array
              lastAttemptAt:
                format: date-time
                type: string
              nextAttemptAt:
                format: date-time
                type: string
              outcome:
                description: Outcome captures the typed result of an action.
                properties:
                  message:
                    type: string
                  type:
                    description: OutcomeType is the typed result of an EventResponse
                      action.
                    enum:
                    - ActionTaken
                    - NoActionNeeded
                    - Skipped
                    - Errored
                    type: string
                required:
                - type
                type: object
              phase:
                description: EventResponsePhase is the lifecycle phase of an EventResponse
                  (design 04 Q2).
                enum:
                - Pending
                - Running
                - Succeeded
                - Failed
                - Cancelled
                type: string
              startedAt:
                format: date-time
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
