---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: attestationbundles.security.ugallu.io
spec:
  group: security.ugallu.io
  names:
    kind: AttestationBundle
    listKind: AttestationBundleList
    plural: attestationbundles
    shortNames:
    - ab
    singular: attestationbundle
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.attestedFor.kind
      name: For
      type: string
    - jsonPath: .spec.attestedFor.name
      name: Subject
      type: string
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .status.signature.mode
      name: Mode
      type: string
    - jsonPath: .status.rekorEntry.logIndex
      name: RekorIndex
      type: integer
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          AttestationBundle is the signed in-toto attestation of a primary fact
          (SecurityEvent or EventResponse). Cluster-scoped.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: |-
              AttestationBundleSpec is the immutable signing assignment.
              One bundle attests one primary fact (design 05 A1: per-fact, not per-incident).
            properties:
              attestedFor:
                description: AttestedFor points at the SecurityEvent or EventResponse
                  being signed.
                properties:
                  apiVersion:
                    description: API version of the referent.
                    type: string
                  fieldPath:
                    description: |-
                      If referring to a piece of an object instead of an entire object, this string
                      should contain a valid JSON/Go field access statement, such as desiredState.manifest.containers[2].
                      For example, if the object reference is to a container within a pod, this would take on a value like:
                      "spec.containers{name}" (where "name" refers to the name of the container that triggered
                      the event) or if no container name is specified "spec.containers[2]" (container with
                      index 2 in this pod). This syntax is chosen only to have some well-defined way of
                      referencing a part of an object.
                    type: string
                  kind:
                    description: |-
                      Kind of the referent.
                      More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
                    type: string
                  name:
                    description: |-
                      Name of the referent.
                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                    type: string
                  namespace:
                    description: |-
                      Namespace of the referent.
                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/namespaces/
                    type: string
                  resourceVersion:
                    description: |-
                      Specific resourceVersion to which this reference is made, if any.
                      More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#concurrency-control-and-consistency
                    type: string
                  uid:
                    description: |-
                      UID of the referent.
                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#uids
                    type: string
                type: object
                x-kubernetes-map-type: atomic
              parentBundleRef:
                description: ParentBundleRef is optional, used for re-attest / closure
                  / redaction record.
                properties:
                  apiVersion:
                    description: API version of the referent.
                    type: string
                  fieldPath:
                    description: |-
                      If referring to a piece of an object instead of an entire object, this string
                      should contain a valid JSON/Go field access statement, such as desiredState.manifest.containers[2].
                      For example, if the object reference is to a container within a pod, this would take on a value like:
                      "spec.containers{name}" (where "name" refers to the name of the container that triggered
                      the event) or if no container name is specified "spec.containers[2]" (container with
                      index 2 in this pod). This syntax is chosen only to have some well-defined way of
                      referencing a part of an object.
                    type: string
                  kind:
                    description: |-
                      Kind of the referent.
                      More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
                    type: string
                  name:
                    description: |-
                      Name of the referent.
                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                    type: string
                  namespace:
                    description: |-
                      Namespace of the referent.
                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/namespaces/
                    type: string
                  resourceVersion:
                    description: |-
                      Specific resourceVersion to which this reference is made, if any.
                      More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#concurrency-control-and-consistency
                    type: string
                  uid:
                    description: |-
                      UID of the referent.
                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#uids
                    type: string
                type: object
                x-kubernetes-map-type: atomic
            required:
            - attestedFor
            type: object
          status:
            description: AttestationBundleStatus is the mutable pipeline state of
              the bundle.
            properties:
              conditions:
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              failures:
                description: Failures records errors encountered during pipeline stages.
                items:
                  description: PipelineFailure records a single stage failure for
                    diagnostics.
                  properties:
                    detail:
                      type: string
                    reason:
                      type: string
                    stage:
                      type: string
                    timestamp:
                      format: date-time
                      type: string
                  required:
                  - stage
                  - timestamp
                  type: object
                type: array
              phase:
                description: 'Phase reflects the pipeline progression: Pending ->
                  Signed -> Logged -> Sealed.'
                enum:
                - Pending
                - Signed
                - Logged
                - Sealed
                - Failed
                type: string
              rekorEntry:
                description: RekorEntry is the transparency log entry for this bundle.
                properties:
                  inclusionProof:
                    description: InclusionProof is the Merkle proof returned by Rekor.
                    properties:
                      hashes:
                        items:
                          type: string
                        type: array
                      logIndex:
                        format: int64
                        type: integer
                      rootHash:
                        type: string
                      treeSize:
                        format: int64
                        type: integer
                    required:
                    - logIndex
                    - treeSize
                    type: object
                  logIndex:
                    format: int64
                    type: integer
                  uuid:
                    type: string
                required:
                - logIndex
                - uuid
                type: object
              sealedAt:
                description: SealedAt is when the bundle reached the terminal Sealed
                  phase.
                format: date-time
                type: string
              signature:
                description: Signature carries metadata about the cryptographic signature
                  applied.
                properties:
                  keyID:
                    type: string
                  mode:
                    description: SigningMode is the cryptographic mode used by the
                      attestor (design 06).
                    enum:
                    - fulcio-keyless
                    - openbao-transit
                    - dual
                    type: string
                required:
                - mode
                type: object
              signedAt:
                description: SignedAt is when signing succeeded.
                format: date-time
                type: string
              statementDigest:
                description: StatementDigest is the sha256 of the canonicalized in-toto
                  Statement.
                type: string
              wormRef:
                description: WormRef points at the DSSE envelope archived to WORM.
                properties:
                  mediaType:
                    type: string
                  sha256:
                    type: string
                  size:
                    format: int64
                    type: integer
                  url:
                    type: string
                required:
                - mediaType
                - url
                type: object
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
